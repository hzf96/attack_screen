# 去除注释可以使用代理进行安装
# proxy=http://127.0.0.1:1080
# https_proxy=http://127.0.0.1:1080

# 去除注释可以使用淘宝源
registry=https://registry.npm.taobao.org

# 去掉注释可以使用 tnpm 源
# registry=http://r.tnpm.oa.com

# 去掉注释可以使用 tencent cloud 源
# registry=https://mirrors.tencent.com/npm/
